async function connectDatabase() {
  // Placeholder for a future database connection.
  // The current auth flow uses the in-memory user model for simplicity.
  console.log("Using in-memory user store. Replace config/db.js with a real database later.");
}

module.exports = {
  connectDatabase,
};